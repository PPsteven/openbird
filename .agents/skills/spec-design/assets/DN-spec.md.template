# Spec N: {TITLE}

> 交付物：{WHAT_GETS_PRODUCED}
> 前置依赖：{DEPS}（已完成或 N/A）

---

## 验证标准

```bash
# 做完后跑以下命令验证，每条必须通过

# 1. {STEP_1_DESCRIPTION}
{EXACT_COMMAND_1}
# → {EXPECTED_OUTPUT_1}

# 2. {STEP_2_DESCRIPTION}
{EXACT_COMMAND_2}
# → {EXPECTED_OUTPUT_2}
```

---

## 文件结构

```
{DIR_STRUCTURE}
```

{NOTE_ABOUT_STRUCTURE}

---

## 核心代码

### {COMPONENT_1}

```javascript
// {FILE_PATH}
// {WHAT_THIS_FILE_DOES}

{ACTUAL_CODE_WITH_FUNCTION_SIGNATURES}
```

### {COMPONENT_2}

```javascript
// {FILE_PATH}
// {WHAT_THIS_FILE_DOES}

{ACTUAL_CODE_WITH_FUNCTION_SIGNATURES}
```

---

## 设计说明

### {DESIGN_TOPIC_1}

{WHY_THIS_APPROACH}: {RATIONALE}

```
{FLOW_DIAGRAM_OR_KEY_LOGIC}
```

### 边界情况

| 场景 | 处理方式 |
|------|----------|
| {EDGE_CASE_1} | {HOW_TO_HANDLE} |
| {EDGE_CASE_2} | {HOW_TO_HANDLE} |

---

## 数据模型

```json
{
  "key": "{KV_KEY_PATTERN}",
  "value": {ACTUAL_JSON_STRUCTURE}
}
```

| Key | Value | TTL | 说明 |
|-----|-------|-----|------|
| `{key_pattern}` | {value_type} | {ttl} | {description} |

---

## 部署/测试

```bash
# {DEPLOYMENT_STEPS}
```
